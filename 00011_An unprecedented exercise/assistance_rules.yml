---
rules:
- !ruby/hash:ActiveSupport::HashWithIndifferentAccess
  when: !ruby/hash:ActiveSupport::HashWithIndifferentAccess
    these_expectations_failed:
    - can_focus Not:UsesIf
  then: there is no need to use an `if` on `can_focus` since we can include the `if`
    condition as part of what we return
